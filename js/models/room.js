app.models = app.models || {};

(function () {
    'use strict';
//TODO: Room not room
    app.models.room = app.models.baseModel.extend({
/*        defaults: {
            name: 'noName',
            floorId: 'noFloor',
            personsCapacity: 'noPersonsCapacity',
            sequence: 'noSequence'
        }*/
    });
})();
