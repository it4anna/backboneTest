app.models = app.models || {};

(function () {
  'use strict';

  // Floor Location
  // ----------

  // A basic **Location** model has `id`, `country` attributes.

  app.models.Location = app.models.baseModel.extend({
  });
})();
